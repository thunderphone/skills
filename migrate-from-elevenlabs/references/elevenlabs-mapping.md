# ElevenLabs mapping review

Review candidates for prompt, first message, language, LLM choice, voice,
webhook tools, supported system tools, knowledge, silence, and maximum duration.

Plan manually for phone numbers, secret values, client-side tools, MCP servers,
and unsupported provider capabilities.

Record source agent/version, item ID, destination agent/revision, chosen voice,
manual gaps, and side-by-side text and audio evidence.
